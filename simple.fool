let
	class Animal(f:bool) {
		fun canFly:bool() f;
	}
	
	var hippo:Animal = new Animal(false);
	var chicken:Animal = new Animal(true);
in

	if(!false)
	then {print(hippo.canFly())}
	else {print(chicken.canFly())};